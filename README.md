# vps_rebuild
Easy way to rebuild your vps without panel

```
apt install -y wget && wget -O os_menu.sh https://raw.githubusercontent.com/Jesanne87/vps_rebuild/main/os_menu.sh && chmod +x os_menu.sh && sed -i -e 's/\r$//' os_menu.sh && ./os_menu.sh
```
